#!/bin/sh

git checkout $1
git checkout main && git pull
git checkout develop && git pull

branch=$(echo $1 | cut -d'/' -f2)

git flow hotfix finish $branch

git co develop && git push
git co main && git push

git co main && cd $PWD